pipeline {
    agent any
        stages {
            stage('Build') {
                agent {
                    docker {
                    image 'anreddy/nginx-image'
                    }
    }
    }
    }
    }